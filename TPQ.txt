1)  How many times does your function get called when you call it with the
input 10?

My function fib gets called 177 times when I input 10 and it returns 55.

2) Consider the sequence of recursive calls that happens. What is a source
of inefficiency?

One source of inefficiency is that some values of fib are being calculated
more than once. What I mean by this is that especially with bigger values
of n, values such as fib(5) can be calculated more than once at different
points of the program leading to a lot more memory usage.
